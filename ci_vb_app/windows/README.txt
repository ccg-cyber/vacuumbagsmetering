// windows/runner/main.cpp stub note
// Run `flutter create --platforms windows .` in the project root to generate
// the full Windows runner files. This requires the Flutter SDK.
// The main.dart and all business logic are platform-independent.
